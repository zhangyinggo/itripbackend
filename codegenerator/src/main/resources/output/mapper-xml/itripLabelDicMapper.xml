<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.kgc.itrip.mapper.ItripLabelDicMapper">

    <select id="getById" resultType="ItripLabelDic">
        select
                id as id,         <!--主键-->
                name as name,         <!--key值-->
                value as value,         <!--value值-->
                description as description,         <!--描述-->
                parentId as parentId,         <!--父级标签id(1级标签则为0)-->
                pic as pic,         <!--标签图片地址-->
                creationDate as creationDate,         <!---->
                createdBy as createdBy,         <!---->
                modifyDate as modifyDate,         <!---->
                modifiedBy as modifiedBy          <!---->
        from itrip_label_dic
        <trim prefix="where" prefixOverrides="and | or">
            <if test="id != null">
                and id=#{id}
            </if>
        </trim>
    </select>

    <select id="getListByMap" resultType="ItripLabelDic"
            parameterType="java.util.Map">
            select
                id as id,      <!--主键-->
                name as name,      <!--key值-->
                value as value,      <!--value值-->
                description as description,      <!--描述-->
                parentId as parentId,      <!--父级标签id(1级标签则为0)-->
                pic as pic,      <!--标签图片地址-->
                creationDate as creationDate,      <!---->
                createdBy as createdBy,      <!---->
                modifyDate as modifyDate,      <!---->
                modifiedBy as modifiedBy       <!---->
            from itrip_label_dic
        <trim prefix="where" prefixOverrides="and | or">
                <if test="id != null and id!=''">
                    and id=#{id}
                </if>
                <if test="name != null and name!=''">
                    and name=#{name}
                </if>
                <if test="value != null and value!=''">
                    and value=#{value}
                </if>
                <if test="description != null and description!=''">
                    and description=#{description}
                </if>
                <if test="parentId != null and parentId!=''">
                    and parentId=#{parentId}
                </if>
                <if test="pic != null and pic!=''">
                    and pic=#{pic}
                </if>
                <if test="creationDate != null and creationDate!=''">
                    and creationDate=#{creationDate}
                </if>
                <if test="createdBy != null and createdBy!=''">
                    and createdBy=#{createdBy}
                </if>
                <if test="modifyDate != null and modifyDate!=''">
                    and modifyDate=#{modifyDate}
                </if>
                <if test="modifiedBy != null and modifiedBy!=''">
                    and modifiedBy=#{modifiedBy}
                </if>
        </trim>
        order by creationDate desc
        <if test="beginPos != null and beginPos!='' and pageSize != null  and pageSize !='' ">
            limit #{beginPos},#{pageSize}
        </if>
    </select>

    <select id="getCountByMap" resultType="Integer" parameterType="java.util.Map">
        select count(1) from itrip_label_dic
        <trim prefix="where" prefixOverrides="and | or">
                    <if test="id != null and id!=''">
                        and id=#{id}
                    </if>
                    <if test="name != null and name!=''">
                        and name=#{name}
                    </if>
                    <if test="value != null and value!=''">
                        and value=#{value}
                    </if>
                    <if test="description != null and description!=''">
                        and description=#{description}
                    </if>
                    <if test="parentId != null and parentId!=''">
                        and parentId=#{parentId}
                    </if>
                    <if test="pic != null and pic!=''">
                        and pic=#{pic}
                    </if>
                    <if test="creationDate != null and creationDate!=''">
                        and creationDate=#{creationDate}
                    </if>
                    <if test="createdBy != null and createdBy!=''">
                        and createdBy=#{createdBy}
                    </if>
                    <if test="modifyDate != null and modifyDate!=''">
                        and modifyDate=#{modifyDate}
                    </if>
                    <if test="modifiedBy != null and modifiedBy!=''">
                        and modifiedBy=#{modifiedBy}
                    </if>
        </trim>
    </select>

    <insert id="save" parameterType="ItripLabelDic">
        insert into itrip_label_dic(
            name ,      <!--key值-->
            value ,      <!--value值-->
            description ,      <!--描述-->
            parentId ,      <!--父级标签id(1级标签则为0)-->
            pic ,      <!--标签图片地址-->
            creationDate ,      <!---->
            createdBy ,      <!---->
            modifyDate ,      <!---->
            modifiedBy       <!---->
        )
        values(
                    #{name} ,      <!--key值-->
                    #{value} ,      <!--value值-->
                    #{description} ,      <!--描述-->
                    #{parentId} ,      <!--父级标签id(1级标签则为0)-->
                    #{pic} ,      <!--标签图片地址-->
                    #{creationDate} ,      <!---->
                    #{createdBy} ,      <!---->
                    #{modifyDate} ,      <!---->
                    #{modifiedBy}       <!---->
        )
    </insert>

    <update id="modify" parameterType="ItripLabelDic">
        update itrip_label_dic
        <trim prefix="set" suffixOverrides="," suffix="where id= #{id}">
                <if test="id != null and id!=''">
                     id=#{id},
                </if>
                <if test="name != null and name!=''">
                     name=#{name},
                </if>
                <if test="value != null and value!=''">
                     value=#{value},
                </if>
                <if test="description != null and description!=''">
                     description=#{description},
                </if>
                <if test="parentId != null and parentId!=''">
                     parentId=#{parentId},
                </if>
                <if test="pic != null and pic!=''">
                     pic=#{pic},
                </if>
                <if test="creationDate != null and creationDate!=''">
                     creationDate=#{creationDate},
                </if>
                <if test="createdBy != null and createdBy!=''">
                     createdBy=#{createdBy},
                </if>
                <if test="modifyDate != null and modifyDate!=''">
                     modifyDate=#{modifyDate},
                </if>
                <if test="modifiedBy != null and modifiedBy!=''">
                     modifiedBy=#{modifiedBy},
                </if>
        </trim>
    </update>

    <delete id="removeById" parameterType="Long">
        delete from itrip_label_dic where id = #{id}
    </delete>
</mapper>